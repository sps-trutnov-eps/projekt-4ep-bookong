# Strong SA password for SQL Server
SA_PASSWORD=ChangeMe_StrongStaging123!

# Include demo data on staging
DEMO_SEED=true

# Optional: clean reseed of demo data (use temporarily, then set back to false)
DEMO_RESET=false

# Master switch: disable migrations/seed step entirely when false
SEED_ENABLED=false
