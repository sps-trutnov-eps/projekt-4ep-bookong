# Strong SA password for SQL Server (change before first deploy!)
SA_PASSWORD=ChangeMe_StrongProduction123!

# Demo data should be disabled in production
DEMO_SEED=false

# Never use clean reseed in production unless you know what you’re doing
DEMO_RESET=false

# Master switch: disable migrations/seed step entirely when false
SEED_ENABLED=true
